<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.yazilimguncelkonular">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/LoginTheme">
        <activity android:name=".dersListesi.icerikDersListesi"
            android:theme="@style/AppTheme"></activity>
        <activity android:name=".Activity.ProfilActivity"
            android:theme="@style/LoginTheme"/>
        <activity android:name=".Activity.MainActivity" />
        <activity
            android:name=".Activity.SideMenu"
            android:label="@string/title_activity_side_menu"
            android:theme="@style/LoginTheme.NoActionBar" />
        <activity android:name=".Activity.GirisActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>