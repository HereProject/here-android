<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.yazilimguncelkonular">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:usesCleartextTraffic="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/LoginTheme">
        <activity
            android:name=".Activity.DersListesi"
            android:label="@string/title_activity_ders_listesi"
            android:configChanges="orientation|screenSize|keyboard"
            android:theme="@style/LoginTheme"></activity>

        <activity
            android:name=".Activity.Anasayfa"
            android:label="@string/title_activity_side_menu"
            android:configChanges="orientation|screenSize|keyboard"
            android:theme="@style/LoginTheme.NoActionBar" />
        <activity android:name=".Activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>